<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/secondaryProgress">
        <shape
            android:innerRadiusRatio="2.5"
            android:shape="ring"
            android:thicknessRatio="11.0"
            android:useLevel="true">

            <gradient
                android:centerColor="@color/text_white"
                android:endColor="@color/text_white"
                android:startColor="@color/text_white"
                android:type="sweep" />
        </shape>
    </item>

    <item android:id="@android:id/progress">
        <rotate
            android:fromDegrees="270"
            android:pivotX="50%"
            android:pivotY="50%"
            android:toDegrees="270">

            <shape
                android:innerRadiusRatio="2.4"
                android:shape="ring"
                android:thicknessRatio="18.0"
                android:useLevel="true">


                <rotate
                    android:fromDegrees="0"
                    android:pivotX="50%"
                    android:pivotY="50%"
                    android:toDegrees="360" />

                <gradient
                    android:centerColor="@color/button_bg_enabled_color"
                    android:endColor="@color/button_bg_enabled_color"
                    android:startColor="@color/button_bg_enabled_color"
                    android:type="sweep" />

            </shape>
        </rotate>
    </item>
</layer-list>